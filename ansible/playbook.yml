---
# LEGACY PLAYBOOK - DEPRECATED
# Questo playbook è stato sostituito dalla struttura organizzata in fasi
# Usa invece: ansible-playbook phases/00-main-playbook.yml

- name: "⚠️  DEPRECATED PLAYBOOK"
  hosts: localhost
  gather_facts: no
  tasks:
    - name: "Display deprecation notice"
      debug:
        msg: |
          ⚠️  QUESTO PLAYBOOK È STATO DEPRECATO
          =====================================
          
          Il progetto è stato riorganizzato in fasi per un deployment più controllato.
          
          🚀 NUOVO COMANDO:
          ansible-playbook phases/00-main-playbook.yml
          
          📁 STRUTTURA ORGANIZZATA:
          phases/01-infrastructure/     - Setup infrastruttura base
          phases/02-kubernetes/         - Cluster Kubernetes (k3s)
          phases/03-kubeedge/           - Integrazione KubeEdge
          phases/04-overlay-network/    - Rete overlay (OVS + Multus)
          phases/05-5g-core/            - 5G Core Network Functions
          phases/06-ueransim-mec/       - UERANSIM e MEC
          
          📖 Per maggiori dettagli: cat phases/README.md
